envSet(MYSQL_INCLUDE_PATH, ${PRODUCT_DIR}/include/mysql)

envPrepend(PATH, ${PRODUCT_DIR}/bin)

pathPrepend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
pathPrepend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
pathPrepend(LSST_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
